# assets/

Arte, sonido y fuentes. Ahora mismo está vacía a propósito: en la Fase 1 todo
se dibuja por código (`_draw()`) para no meter binarios en el repo antes de
tener decidido el estilo visual.

Cuando se añadan texturas, recuerda que `.gitignore` excluye los `*.import`:
Godot los regenera solo al abrir el proyecto.
